import React from "react";

import { Text, TouchableOpacity } from "react-native";

const Filters = () => {
	return (
		<TouchableOpacity >
			<Text >
			 Filters
			</Text>
		</TouchableOpacity>
	);
};

export default Filters;
